-- src/filter/conditions/from_netlist.moon
-- Condition : l'adresse IP source appartient à une netlist nommée.
-- Port direct de shelterfilter conditions/from_netlist.moon.

(require "filter.conditions._match_netlist") "src_ip"
