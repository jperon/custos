config custos 'main'
	option enabled           '1'

	# Mise à jour automatique des listes de domaines via cron
	# Lancer manuellement : /etc/init.d/custos update_lists
	option update_lists_enabled  '1'
	option update_lists_schedule '0 3 * * *'
